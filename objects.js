var playlist = new Object({ artistName: 'songTitles' });

function updatePlaylist(playlist, artistName, songTitle)
{



  return  return Object.assign({}, obj, { [key]: value });
}

function removeFromPlaylist(playlist, )
